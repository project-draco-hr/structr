{
  final StringBuilder buf=new StringBuilder();
  final int len=values.length;
  for (int i=0; i < len; i++) {
    if (values[i] != null) {
      buf.append(values[i].toString());
    }
    if (i < len - 1) {
      buf.append(", ");
    }
  }
  return buf.toString();
}
