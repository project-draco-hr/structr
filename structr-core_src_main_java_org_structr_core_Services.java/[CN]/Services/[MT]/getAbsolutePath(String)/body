{
  if (path.startsWith("/")) {
    return (path);
  }
  StringBuilder ret=new StringBuilder();
  ret.append(basePath);
  ret.append(basePath.endsWith("/") ? "" : "/");
  ret.append(path);
  return (ret.toString());
}
