{
  R instance=(R)instanceCache.get(type);
  if (instance == null) {
    try {
      instance=type.newInstance();
      instanceCache.put(type,instance);
    }
 catch (    Throwable t) {
      t.printStackTrace();
    }
  }
  return instance;
}
