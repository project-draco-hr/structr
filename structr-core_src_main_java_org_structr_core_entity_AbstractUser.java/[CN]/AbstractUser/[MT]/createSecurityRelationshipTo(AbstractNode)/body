{
  final App app=StructrApp.getInstance();
  try {
    app.beginTx();
    final Security rel=app.create(this,obj,Security.class);
    app.commitTx();
    return rel;
  }
  finally {
    app.finishTx();
  }
}
