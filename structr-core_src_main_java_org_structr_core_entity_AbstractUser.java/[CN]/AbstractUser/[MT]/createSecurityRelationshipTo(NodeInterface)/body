{
  final App app=StructrApp.getInstance();
  try {
    app.beginTx();
    final Security rel=app.create((Principal)this,obj,Security.class);
    app.commitTx();
    return rel;
  }
  finally {
    app.finishTx();
  }
}
