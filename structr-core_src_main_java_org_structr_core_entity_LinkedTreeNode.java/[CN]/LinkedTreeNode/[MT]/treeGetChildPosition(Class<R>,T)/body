{
  final R rel=child.getIncomingRelationship(type);
  if (rel != null) {
    Integer pos=rel.getProperty(positionProperty);
    if (pos != null) {
      return pos.intValue();
    }
  }
  return 0;
}
