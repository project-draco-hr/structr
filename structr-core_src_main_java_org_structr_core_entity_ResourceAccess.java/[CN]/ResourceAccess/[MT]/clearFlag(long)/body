{
  App app=StructrApp.getInstance(securityContext);
  try {
    app.beginTx();
    cachedFlags=null;
    setProperty(ResourceAccess.flags,getFlags() & ~flag);
    app.commitTx();
  }
  finally {
    app.finishTx();
  }
}
