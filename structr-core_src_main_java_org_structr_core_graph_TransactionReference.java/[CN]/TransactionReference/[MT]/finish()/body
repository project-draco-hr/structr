{
  if (--referenceCount == 0) {
    if (!successful) {
      tx.failure();
    }
    tx.finish();
    for (int i=0; i < referenceCount; i++) {
      System.out.print("    ");
    }
    System.out.println("FINISH(" + Thread.currentThread().getId() + ")");
  }
}
