{
  if (--referenceCount == 0) {
    for (int i=0; i < referenceCount; i++) {
      System.out.print("    ");
    }
    System.out.println("FINISH");
    if (!successful) {
      tx.failure();
    }
    tx.finish();
  }
}
