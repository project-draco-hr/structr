{
  if (array != null && array.length >= minLength && array.length <= maxLength) {
    for (    final Object element : array) {
      if (element == null) {
        return false;
      }
    }
    return true;
  }
  return false;
}
