{
  final StringBuilder buf=new StringBuilder();
  buf.append(m);
  buf.append("(");
  buf.append(obj);
  for (  final String arg : args) {
    buf.append(", ");
    buf.append(arg);
  }
  buf.append(")");
  return buf.toString();
}
