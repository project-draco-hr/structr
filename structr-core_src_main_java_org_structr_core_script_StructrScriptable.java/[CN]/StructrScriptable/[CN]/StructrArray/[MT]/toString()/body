{
  final StringBuilder buf=new StringBuilder();
  boolean first=true;
  buf.append("[");
  for (  final Object obj : this.toArray()) {
    if (!first) {
      buf.append(",");
      first=false;
    }
    buf.append(obj.toString());
  }
  buf.append("]");
  return buf.toString();
}
