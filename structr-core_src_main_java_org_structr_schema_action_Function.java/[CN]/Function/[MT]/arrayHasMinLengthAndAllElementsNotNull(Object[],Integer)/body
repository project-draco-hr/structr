{
  if (array != null && minLength != null && array.length >= minLength) {
    for (    final Object element : array) {
      if (element == null) {
        return false;
      }
    }
    return true;
  }
  return false;
}
