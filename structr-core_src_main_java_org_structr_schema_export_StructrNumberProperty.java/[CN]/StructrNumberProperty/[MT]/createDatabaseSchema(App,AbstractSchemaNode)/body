{
  final SchemaProperty schemaProperty=app.create(SchemaProperty.class,new NodeAttribute(SchemaProperty.schemaNode,schemaNode),new NodeAttribute(AbstractNode.name,getName()));
  setDefaultProperties(schemaProperty);
  schemaProperty.setProperty(SchemaProperty.propertyType,Type.Double.name());
  return schemaProperty;
}
