{
  if (cascade != null) {
switch (cascade) {
case sourceToTarget:
      return Relation.SOURCE_TO_TARGET;
case targetToSource:
    return Relation.TARGET_TO_SOURCE;
case always:
  return Relation.ALWAYS;
}
}
return 0;
}
