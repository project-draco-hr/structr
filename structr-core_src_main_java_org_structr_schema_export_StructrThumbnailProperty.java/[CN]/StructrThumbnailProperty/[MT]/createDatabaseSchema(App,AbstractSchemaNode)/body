{
  final SchemaProperty property=super.createDatabaseSchema(app,schemaNode);
  property.setProperty(SchemaProperty.propertyType,Type.Thumbnail.name());
  property.setProperty(SchemaProperty.format,getFormat());
  return property;
}
