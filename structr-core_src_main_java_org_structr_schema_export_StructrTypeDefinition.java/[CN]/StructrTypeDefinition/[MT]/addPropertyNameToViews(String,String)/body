{
  for (  final String viewName : views) {
    Set<String> view=this.views.get(viewName);
    if (view == null) {
      view=new TreeSet<>();
      this.views.put(viewName,view);
    }
    view.add(name);
  }
}
