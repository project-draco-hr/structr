{
  try {
    return Integer.parseInt(source);
  }
 catch (  Throwable t) {
    errorBuffer.add(Schema.class.getSimpleName(),new InvalidPropertySchemaToken(source,"invalid_" + which + "_bound",StringUtils.capitalize(which) + " bound must be of type Integer."));
  }
  return null;
}
