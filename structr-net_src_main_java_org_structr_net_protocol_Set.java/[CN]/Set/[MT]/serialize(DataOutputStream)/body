{
  super.serialize(dos);
  serializeUUID(dos,recipient);
  serializeUUID(dos,objectId);
  serializeUUID(dos,transactionId);
  dos.writeUTF(key);
  serializeObject(dos,value);
  time.serialize(dos);
}
