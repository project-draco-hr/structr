{
  if (StringUtils.isNotBlank(line)) {
    final String[] parts=line.split("[\\s]+");
    if (parts.length == 2) {
      username=parts[0];
      password=parts[1];
    }
  }
 else {
    username=null;
    password=null;
  }
  return true;
}
