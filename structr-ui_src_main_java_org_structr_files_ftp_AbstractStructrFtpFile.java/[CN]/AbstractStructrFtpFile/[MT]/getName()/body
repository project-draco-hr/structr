{
  String name=structrFile != null ? structrFile.getProperty(File.name) : (newPath.contains("/") ? StringUtils.substringAfterLast(newPath,"/") : newPath);
  return name == null ? structrFile.getUuid() : name;
}
