{
  if (!folders.isEmpty()) {
    term.println();
    for (    final Folder folder : folders) {
      term.print(folder.getName() + "/  ");
    }
    term.println();
    parent.displayPrompt();
    term.print(line);
  }
}
