{
  instantiateConverter(currentObject);
  try {
    return converter != null ? converter.convert(source) : source;
  }
 catch (  FrameworkException ex) {
    logger.log(Level.SEVERE,null,ex);
  }
  return null;
}
