{
  String mode=(String)attributes.get("mode");
  if (mode == null) {
    mode="dir";
  }
switch (mode) {
case "dir":
    deployFromDirectory(attributes);
  break;
case "file":
deployFromFile(attributes);
break;
}
}
