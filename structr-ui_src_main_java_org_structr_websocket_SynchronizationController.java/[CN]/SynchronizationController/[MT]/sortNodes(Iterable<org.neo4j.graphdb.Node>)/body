{
  ArrayList<org.neo4j.graphdb.Node> list=new ArrayList<>();
  for (  org.neo4j.graphdb.Node p : it) {
    list.add(p);
  }
  Collections.sort(list,new Comparator<org.neo4j.graphdb.Node>(){
    @Override public int compare(    org.neo4j.graphdb.Node o1,    org.neo4j.graphdb.Node o2){
      Long id1=o1.getId();
      Long id2=o2.getId();
      return id1.compareTo(id2);
    }
  }
);
  return list;
}
