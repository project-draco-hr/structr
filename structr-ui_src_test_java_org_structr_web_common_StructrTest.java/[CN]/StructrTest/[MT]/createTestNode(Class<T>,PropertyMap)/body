{
  props.put(AbstractNode.type,type.getSimpleName());
  try {
    app.beginTx();
    final T newNode=app.create(type,props);
    app.commitTx();
    return newNode;
  }
  finally {
    app.finishTx();
  }
}
