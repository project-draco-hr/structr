{
  try {
    List<T> nodes=new LinkedList<>();
    app.beginTx();
    for (int i=0; i < number; i++) {
      nodes.add(app.create(type,props));
    }
    app.commitTx();
    return nodes;
  }
  finally {
    app.finishTx();
  }
}
