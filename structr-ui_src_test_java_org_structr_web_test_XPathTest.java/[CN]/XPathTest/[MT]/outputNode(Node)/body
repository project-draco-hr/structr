{
  StringBuilder buf=new StringBuilder();
  if (node != null) {
    buf.append(node.getNodeName());
    buf.append(" (");
    buf.append(node.getNodeType());
    buf.append(")");
  }
  return buf.toString();
}
