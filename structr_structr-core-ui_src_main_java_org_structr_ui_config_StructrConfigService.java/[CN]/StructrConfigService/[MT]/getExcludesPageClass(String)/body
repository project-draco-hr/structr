{
  for (int i=0; i < excludesList.size(); i++) {
    StructrConfigService.ExcludesElm override=(StructrConfigService.ExcludesElm)excludesList.get(i);
    if (override.isMatch(path)) {
      return override.getPageClass();
    }
  }
  return null;
}
