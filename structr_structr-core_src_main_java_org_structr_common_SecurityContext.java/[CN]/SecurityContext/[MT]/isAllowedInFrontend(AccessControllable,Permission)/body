{
  if (node == null) {
    return false;
  }
  user=getUser();
  if ((user != null) && (user instanceof SuperUser)) {
    return true;
  }
switch (permission) {
case Read:
    return isVisibleInFrontend(node);
default :
  return false;
}
}
