{
  if (node == null) {
    return false;
  }
  Principal user=getUser();
  if (user == null) {
    return false;
  }
  if (user instanceof SuperUser) {
    return true;
  }
  if (isAllowedInBackend(node,Permission.read)) {
    return true;
  }
  return false;
}
