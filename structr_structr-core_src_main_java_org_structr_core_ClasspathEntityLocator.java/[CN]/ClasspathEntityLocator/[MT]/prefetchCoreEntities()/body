{
  logger.log(Level.FINE,"Prefetching core entities");
  Class[] prefetchEntities={StructrNode.class,Command.class,Service.class};
  for (int i=0; i < prefetchEntities.length; i++) {
    locateEntitiesByType(prefetchEntities[i]);
  }
}
