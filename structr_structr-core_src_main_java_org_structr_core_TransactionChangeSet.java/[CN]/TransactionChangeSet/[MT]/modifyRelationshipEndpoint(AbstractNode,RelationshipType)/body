{
  if (relationshipType instanceof RelType) {
switch ((RelType)relationshipType) {
case OWNS:
      modifyOwner(node);
    break;
case SECURITY:
  modifySecurity(node);
break;
case IS_AT:
modifyLocation(node);
break;
default :
modify(node);
break;
}
}
 else {
modify(node);
}
}
