{
  ConnectionListener listener=new ConnectionListener(connection);
  listeners.put(connection,listener);
  activeTransmissions.add(listener);
  Services.command(AddGlobalNotificationCommand.class).execute(new DefaultNotification("Incoming transmission from " + connection.getRemoteAddressTCP(),null));
}
