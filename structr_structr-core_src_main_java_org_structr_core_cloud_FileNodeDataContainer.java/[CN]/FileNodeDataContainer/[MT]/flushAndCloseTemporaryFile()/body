{
  if (outputStream != null) {
    try {
      outputStream.flush();
      outputStream.close();
    }
 catch (    Throwable t) {
      t.printStackTrace();
    }
  }
 else {
    logger.log(Level.WARNING,"outputStream was null!");
  }
}
