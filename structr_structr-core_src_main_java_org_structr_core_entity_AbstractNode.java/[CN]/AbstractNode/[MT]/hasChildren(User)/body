{
  List<StructrRelationship> childRels=getOutgoingChildRelationships();
  List<StructrRelationship> linkRels=getOutgoingLinkRelationships();
  return (linkRels != null && !(linkRels.isEmpty()) && childRels != null && !(childRels.isEmpty()));
}
