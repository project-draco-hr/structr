{
  if (dbNode == null) {
    return "AbstractNode with null database node";
  }
  return (dbNode.hasProperty(Key.name.name()) ? dbNode.getProperty(Key.name.name()) : "<AbstractNode>") + " [" + (dbNode.hasProperty(Key.type.name()) ? dbNode.getProperty(Key.type.name()) : "Unknown")+ ", "+ dbNode.getId()+ "]";
}
