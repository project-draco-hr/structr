{
  Object ret=null;
  if (dbNode.hasProperty(key)) {
    if (key.equals(AbstractNode.CREATED_DATE_KEY) || key.equals(AbstractNode.LAST_MODIFIED_DATE_KEY)) {
      ret=new Date((Long)dbNode.getProperty(key));
    }
 else {
      ret=dbNode.getProperty(key);
    }
  }
  return ret;
}
