{
  Object ret=null;
  if (dbNode.hasProperty(key)) {
    if (key.equals(StructrNode.CREATED_DATE_KEY) || key.equals(StructrNode.LAST_MODIFIED_DATE_KEY)) {
      ret=new Date((Long)dbNode.getProperty(key));
    }
 else {
      ret=dbNode.getProperty(key);
    }
  }
  return ret;
}
