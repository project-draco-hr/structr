{
  AbstractComponent root=new Container();
  root.add(new Paragraph().add(new Content("This is a Trash node. You can drag and drop other nodes into it,","and recover them later, or empty the trash to delete ","the nodes permanently.")));
  return (root);
}
