{
  if (isRunning()) {
    for (    RunnableService s : registeredServices) {
      s.stopService();
    }
    waitFor(registeredServices.isEmpty());
    graphDb.shutdown();
    graphDb=null;
    isInitialized=false;
  }
}
