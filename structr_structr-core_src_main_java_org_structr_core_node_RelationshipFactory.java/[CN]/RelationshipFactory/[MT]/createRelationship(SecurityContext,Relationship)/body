{
  String nodeType=relationship.hasProperty(AbstractNode.Key.type.name()) ? (String)relationship.getProperty(AbstractNode.Key.type.name()) : "";
  return createRelationship(securityContext,relationship,nodeType);
}
