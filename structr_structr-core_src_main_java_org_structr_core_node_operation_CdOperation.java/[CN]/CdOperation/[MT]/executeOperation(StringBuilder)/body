{
  if (target != null) {
    try {
      AbstractNode newLocation=(AbstractNode)Services.command(securityContext,FindNodeCommand.class).execute(currentNode,target);
      if (newLocation != null) {
        for (        Callback callback : callbacks) {
          callback.callback(newLocation.getId());
        }
        return (true);
      }
    }
 catch (    FrameworkException fex) {
      fex.printStackTrace();
    }
  }
 else {
    throw new NodeCommandException("No parameter");
  }
  return (false);
}
