{
  if (target != null) {
    AbstractNode newLocation=(AbstractNode)Services.command(FindNodeCommand.class).execute(CurrentRequest.getCurrentUser(),currentNode,target);
    if (newLocation != null) {
      for (      Callback callback : callbacks) {
        callback.callback(newLocation.getId());
      }
      return (true);
    }
  }
 else {
    throw new NodeCommandException("No parameter");
  }
  return (false);
}
