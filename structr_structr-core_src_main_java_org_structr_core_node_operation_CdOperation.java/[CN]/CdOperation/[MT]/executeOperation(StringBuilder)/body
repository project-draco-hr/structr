{
  if (target != null) {
    AbstractNode newLocation=(AbstractNode)Services.command(FindNodeCommand.class).execute(CurrentSession.getUser(),currentNode,target);
    if (newLocation != null) {
      for (      Callback callback : callbacks) {
        callback.callback(newLocation.getId());
      }
      return (true);
    }
  }
 else {
    throw new NodeCommandException("No parameter");
  }
  return (false);
}
