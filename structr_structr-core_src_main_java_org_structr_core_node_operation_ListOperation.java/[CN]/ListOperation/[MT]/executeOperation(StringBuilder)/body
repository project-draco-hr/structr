{
  if (nodeList.isEmpty()) {
    nodeList.add(currentNode);
  }
  stdOut.append("<table class=\"file-list\">");
  for (  AbstractNode node : nodeList) {
    list(stdOut,node,0);
  }
  stdOut.append("</table>");
  return (true);
}
