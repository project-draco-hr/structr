{
  return new Adapter<List<T>,List<GraphObject>>(){
    @Override public List<GraphObject> adapt(    List<T> s) throws FrameworkException {
      List<GraphObject> list=new LinkedList<GraphObject>();
      for (      T t : s) {
        list.add(deserializationStrategy.deserialize(securityContext,type,t));
      }
      return list;
    }
  }
;
}
