{
  ResourceConstraint combinedConstraint=null;
  if (next instanceof IdConstraint)   combinedConstraint=new TypedIdConstraint(type,((IdConstraint)next).getId());
 else   if (next instanceof SearchConstraint)   combinedConstraint=new TypedSearchConstraint(type,((SearchConstraint)next).getSearchString());
  return combinedConstraint;
}
