{
  ResourceConstraint combinedConstraint=null;
  if (next instanceof IdConstraint)   combinedConstraint=new TypedIdConstraint((IdConstraint)next,this);
 else   if (next instanceof SearchConstraint)   combinedConstraint=new TypedSearchConstraint(this,((SearchConstraint)next).getSearchString());
  return combinedConstraint;
}
