{
  if (next instanceof IdConstraint)   return new TypedIdConstraint((IdConstraint)next,this);
 else   if (next instanceof SearchConstraint)   return new TypedSearchConstraint(this,((SearchConstraint)next).getSearchString());
  return super.tryCombineWith(next);
}
