{
  StringBuilder buf=new StringBuilder(100);
  buf.append("{\n");
  if (message != null) {
    buf.append("    \"message\" : \"").append(message).append("\"\n");
  }
 else {
    buf.append("    \"message\" : \"\"\n");
  }
  buf.append("}\n");
  return buf.toString();
}
