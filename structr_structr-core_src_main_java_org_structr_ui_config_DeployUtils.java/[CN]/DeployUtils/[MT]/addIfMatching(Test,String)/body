{
  try {
    if (test.matches(fqn)) {
      matches.add(fqn);
    }
  }
 catch (  Throwable t) {
    logService.error("could not examine class '" + fqn + "'"+ " due to a "+ t.getClass().getName()+ " with message: "+ t.getMessage());
  }
}
