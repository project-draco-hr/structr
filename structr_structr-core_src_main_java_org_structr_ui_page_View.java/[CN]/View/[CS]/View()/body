{
  super();
  contextPath=getContext().getRequest().getContextPath().concat(getContext().getPagePath(View.class).concat("?").concat(NODE_ID_KEY.concat("=")));
  CurrentRequest.setAccessMode(AccessMode.Frontend);
}
