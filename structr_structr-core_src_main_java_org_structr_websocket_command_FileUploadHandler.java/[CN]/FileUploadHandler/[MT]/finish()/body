{
  try {
    FileChannel channel=getChannel();
    if (channel != null) {
      channel.force(true);
      channel.close();
    }
  }
 catch (  Throwable t) {
    logger.log(Level.WARNING,"Unable to finish file upload",t);
  }
}
