{
  Map<String,Slot> ret=new LinkedHashMap<String,Slot>();
  ret.put(Key.username.name(),new StringSlot());
  ret.put(Key.password.name(),new StringSlot());
  ret.put(Key.antiRobot.name(),new NullSlot(String.class));
  return (ret);
}
