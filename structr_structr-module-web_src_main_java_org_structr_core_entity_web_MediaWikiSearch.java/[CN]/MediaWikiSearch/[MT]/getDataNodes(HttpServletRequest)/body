{
  ServletContext context=securityContext.getSession().getServletContext();
  List<AbstractNode> dataNodes=null;
  if (context != null) {
    TemporaryValue<List<AbstractNode>> value=(TemporaryValue<List<AbstractNode>>)context.getAttribute(CACHED_WIKI_CONTENT.concat(this.getIdString()));
    dataNodes=getContentFromSource(request);
  }
  return dataNodes;
}
