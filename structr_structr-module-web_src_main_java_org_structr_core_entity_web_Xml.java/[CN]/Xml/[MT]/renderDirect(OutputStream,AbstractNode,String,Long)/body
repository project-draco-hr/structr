{
  if (isVisible()) {
    try {
      StringReader in=new StringReader(getXml());
      IOUtils.copy(in,out);
    }
 catch (    IOException e) {
      System.out.println("Error while rendering " + getXml() + ": "+ e.getMessage());
    }
  }
}
