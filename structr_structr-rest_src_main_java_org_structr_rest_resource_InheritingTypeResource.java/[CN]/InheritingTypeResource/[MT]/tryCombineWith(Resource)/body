{
  if (next instanceof IdResource)   return new InheritingTypedIdResource(securityContext,(IdResource)next,this);
 else   if (next instanceof TypeResource)   throw new IllegalPathException();
  return super.tryCombineWith(next);
}
